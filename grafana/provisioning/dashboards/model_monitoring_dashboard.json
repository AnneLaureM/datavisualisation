{
  "annotations": {
    "list": []
  },
  "editable": true,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "type": "timeseries",
      "title": "y_true and y_pred",
      "datasource": "TimescaleDB",
      "targets": [
        {
          "format": "time_series",
          "rawSql": "SELECT time, y_true, y_pred FROM model_metrics WHERE $__timeFilter(time) ORDER BY time",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 9,
        "w": 24,
        "x": 0,
        "y": 0
      }
    },
    {
      "type": "timeseries",
      "title": "Anomaly rate (1m)",
      "datasource": "TimescaleDB",
      "targets": [
        {
          "format": "time_series",
          "rawSql": "SELECT $__timeGroup(time,'1m') AS time, AVG(anomaly_flag) AS anomaly_rate FROM model_metrics WHERE $__timeFilter(time) GROUP BY 1 ORDER BY 1",
          "refId": "B"
        }
      ],
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 9,
        "w": 12,
        "x": 0,
        "y": 9
      }
    },
    {
      "type": "timeseries",
      "title": "Drift score",
      "datasource": "TimescaleDB",
      "targets": [
        {
          "format": "time_series",
          "rawSql": "SELECT time, drift_score FROM model_metrics WHERE $__timeFilter(time) ORDER BY time",
          "refId": "C"
        }
      ],
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "gridPos": {
        "h": 9,
        "w": 12,
        "x": 12,
        "y": 9
      }
    }
  ],
  "refresh": "10s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": [
    "monitoring",
    "timescale"
  ],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-1h",
    "to": "now"
  },
  "title": "Model Monitoring (TimescaleDB)",
  "uid": "model-monitoring-timescale",
  "version": 1
}